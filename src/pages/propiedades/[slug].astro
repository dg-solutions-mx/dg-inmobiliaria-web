---
import Layout from '@/layouts/Layout.astro';
import { supabase } from '@/lib/supabase';

export async function getStaticPaths() {
  // const { data } = await supabase.from('propiedades').select('slug');
  // return (data ?? []).map(({ slug }) => ({ params: { slug } }));
  return [];
}

const { slug } = Astro.params;

// const { data: propiedad } = await supabase
//   .from('propiedades')
//   .select('*')
//   .eq('slug', slug)
//   .single();
const propiedad: any = null;

if (!propiedad) return Astro.redirect('/404');
---

<Layout
  title={propiedad.title}
  description={propiedad.description}
  ogImage={propiedad.image_url}
>
  <article class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <h1 class="text-4xl font-bold text-gray-900">{propiedad.title}</h1>
    <!-- Detalle de propiedad -->
  </article>
</Layout>
